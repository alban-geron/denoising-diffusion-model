# Advanced machine learning project github. Diffusion models and GANS
All the code to reproduce the results is located in the folder reproducibility. make sure to run the code on a gpu if you want to sample new images as the forward passes are quite costly on a cpu. generation can take anywhere between 10 seconds to a minute and 40s depending on the model generating.
